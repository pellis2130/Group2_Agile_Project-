#include <iostream>
#include "calculateTwo.h"

using namespace std;

void calculateTwo() {
    double a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b;
    cout << "Product = " << (a * b) << endl;
}