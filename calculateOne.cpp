#include <iostream>
#include "calculateOne.h"

using namespace std;

void calculateOne() {
    double a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b;
    cout << "Sum = " << (a + b) << endl;
}